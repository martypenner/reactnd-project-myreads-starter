## Prerequisites

* [npm](https://nodejs.org/en/download/)

## Installation

* `git clone <repository-url>` this repository
* `cd` into the new directory
* `npm install`

## Running / Development

* `npm start`
